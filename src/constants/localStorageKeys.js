/**
 * Keys for localStorage's data
 */

/**
 * @const {string} User's access token
 */
export const ACCESS_TOKEN = 'ACCESS_TOKEN';

/**
 * @const {string} User's workspaces
 */
export const WORKSPACES = 'WORKSPACES';
