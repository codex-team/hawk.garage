/**
 * Options for TooltipMenu component
 */
export interface TooltipMenuOptions {
  /**
   * Item title
   */
  title: string;

  /**
   * Action to perform on item click
   */
  onClick: Function
}
